server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        # Эта строка критически важна для React:
        # если путь не найден как файл, отдаем index.html
        try_files $uri $uri/ /index.html;
    }
}